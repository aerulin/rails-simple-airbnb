

<% @flats.each do |flat| %>
  <div class="card">
    <div class="card-header">
      <img src= <%= flat.image %> alt="picutre" />
    </div>
    <div class="card-body">
      <h2 class="title"><%= flat.name %></h2>
      <h4 class="address"><%= flat.address %></h4>
      <p class="tag tag-teal">💂‍♀️<%= flat.number_of_guests %></p>
      <p class="tag tag-teal">💸<%= flat.price_per_night %></p>
      <a href="" class="btn">Book now</a>

        </div>
      </div>
    </div>
   <% end %>
  </div>
